pipeline {
    agent any

    stages {
        stage('testing time intervel  Branch') {
            steps {
                echo "Testing periodically trigger  branch"
                sh 'echo testing step here'
            }
        }
    }
}
