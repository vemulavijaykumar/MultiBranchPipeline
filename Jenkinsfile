pipeline {
    agent any

    stages {
        stage('Dev Branch') {
            steps {
                echo "Testing Dev branch"
                sh 'echo Dev scanning new step here'
            }
        }
    }
}
