pipeline {
    agent any

    stages {
        stage('Master') {
            steps {
                echo "Testing master branch"
                sh 'echo Master step here'
            }
        }
    }
}
